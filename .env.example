DATABASE_URL="postgresql://user:password@localhost:5432/resik_db?schema=public"

AUTH_SECRET="generate-dengan: openssl rand -base64 32"

# whatsapp-service (proses Node terpisah, lihat whatsapp-service/README) — isi setelah
# service jalan & QR sudah di-scan
# WHATSAPP_SERVICE_URL="http://localhost:4001"
# WHATSAPP_SERVICE_TOKEN="samakan-dengan-SERVICE_TOKEN-di-whatsapp-service/.env"

